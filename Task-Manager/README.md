# Task Manager (CLI)

Simple CLI task manager example with tests.

How to run

- Run CLI: `go run cli-tm.go`
- Run tests: `go test` (see `cli-tm_test.go`)

Build

- `go build -o task-manager .`

Notes

- Uses Go modules. The tests demonstrate expected CLI behavior.
