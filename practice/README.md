# Practice

Small practice project for experimenting with Go code snippets.

How to run

- Run: `go run main.go`
- Build: `go build -o practice .`

Notes

- A lightweight playground; modify `main.go` to try ideas quickly.
- Uses modules (see `go.mod`).
