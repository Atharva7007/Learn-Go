# Concurrent Log Filter

Small demo showing concurrent log filtering in Go.

How to run

- Build and run: `go run main.go`
- Build binary: `go build -o concurrent-log-filter .`

Notes

- Uses Go modules (see `go.mod`).
- Intended as a concise example of goroutines and channels for log processing.
